import { PackageGridPage } from '../../../components/catalog/package-grid-page';

export default function MealBoxesPage() {
  return <PackageGridPage type="MEAL_BOX" title="Meal Boxes" description="Complete, individually portioned meals. Filter by diet, review every included dish, and select a box in one click." />;
}
