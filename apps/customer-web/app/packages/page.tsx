import { PackageGridPage } from '../../components/catalog/package-grid-page';

export default function PackagesPage() {
  return <PackageGridPage type="PACKAGES" title="Catering Packages" description="Compare the essentials at a glance. Select a package to add your event details and review its complete menu." />;
}
